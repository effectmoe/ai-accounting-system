name = "accounting-email-tracker"
main = "src/index.ts"
compatibility_date = "2024-12-01"

# 本番環境では以下を設定（wrangler d1 create後に更新）
# [[d1_databases]]
# binding = "DB"
# database_name = "accounting-email-events"
# database_id = "YOUR_DATABASE_ID_HERE"

# 環境変数
[vars]
CORS_ORIGIN = "https://accounting-automation.vercel.app"

# 開発環境
[env.dev]
vars = { CORS_ORIGIN = "http://localhost:3000" }

# カスタムドメイン設定（オプション）
# routes = [
#   { pattern = "tracker.effect.moe/*", zone_name = "effect.moe" }
# ]
